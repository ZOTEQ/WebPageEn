# SECTION 模块顺序调整计划

## 一、需求理解

用户要求调整页面中三个 SECTION 模块的前后顺序。

### 当前页面模块结构（按顺序）

| 序号 | 模块ID | 模块名称 | 描述 |
|------|--------|----------|------|
| 1 | `hero` | 首屏英雄区 | 页面顶部大图，包含标题和描述 |
| 2 | `bases` | 生产基地 | 重庆和马来西亚工厂展示 |
| 3 | `labs` | 实验室 | 四大实验室介绍 |
| 4 | `sites` | 分销站点 | 中国四大分销站点 |
| 5 | `equipment` | 设备BENTO | 八大实验设备展示 |
| 6 | `alibaba` | Alibaba商店 | 沉浸式大图展示 |

---

## 二、用户需求分析

用户提到"三个 SECTION 模块"，但未明确指出具体是哪三个。

### 可能的调整方案

#### 方案A：调整中间三个模块
```
hero → [bases, labs, sites] → equipment → alibaba
```
调整 bases、labs、sites 的顺序

#### 方案B：调整底部三个模块
```
hero → bases → labs → [sites, equipment, alibaba]
```
调整 sites、equipment、alibaba 的顺序

#### 方案C：自定义选择
用户指定任意三个模块进行顺序调整

---

## 三、执行计划

### 步骤1：确认用户需求
需要用户明确指出要调整哪三个模块以及期望的新顺序

### 步骤2：备份文件
```bash
cp FacilityInfo.html backups/FacilityInfo.html.bak
```

### 步骤3：调整模块顺序
通过剪切粘贴方式调整指定的三个 SECTION 模块位置

### 步骤4：测试验证
刷新页面确认模块顺序正确

---

## 四、风险与依赖

### 风险
- 模块顺序调整可能影响页面逻辑（如锚点链接）
- CSS样式可能需要调整以适应新顺序

### 解决方案
- 更新导航菜单中的锚点链接顺序
- 检查并修复相关的CSS样式

---

## 五、文件清单

| 文件 | 类型 | 修改 |
|------|------|------|
| `FacilityInfo.html` | HTML | 修改 |
| `backups/FacilityInfo.html.bak` | 备份 | 创建 |

---

## 六、待确认信息

**需要用户确认：**
1. 需要调整哪三个模块？（请列出模块ID）
2. 期望的新顺序是什么？

示例格式：
```
调整模块：bases, labs, sites
新顺序：labs → bases → sites
```

---

*请用户确认具体要调整的模块和顺序后，即可执行修改。*